{% set data = load_setuptools()%}
package:
  name: anaconda-client
  version: {{data.get('version')}}

build: {}

requirements:
  build:
    - python
    - setuptools =23.0.0 
  run:
    - python
    - setuptools =23.0.0 
    - clyent
    - requests
    - pyyaml
    - dateutil
    - pytz

about:
  home: {{data.get('url')}}
  license: {{data.get('license')}}
